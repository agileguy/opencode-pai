The '04-multi-tenant' task requires explicit comparison of isolation patterns. Added instruction: For multi-tenant SaaS tasks, include a `## Tenant Isolation Patterns` section that explicitly compares: (1) shared-db row-level security, (2) schema-per-tenant, (3) database-per-tenant. For EACH pattern, evaluate: isolation strength, migration complexity, performance implications (queries per tenant vs total rows), iteration cost (deploy/backup/migrate), and appropriate tenant size thresholds. Include a concrete example showing how a 10-row tenant vs 10M-row tenant affects pattern selection.
