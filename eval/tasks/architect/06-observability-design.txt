Design the observability stack for a microservice handling 5,000 requests/second with a p99 latency SLA of 200ms. The service calls 3 downstream dependencies and uses PostgreSQL. Write your design to the output directory as observability.md. Cover: metrics (what to measure, alerting thresholds), structured logging (what to log at each level), distributed tracing (propagation, sampling rate), dashboards (what to show), and on-call runbooks (top 5 likely incidents). Include cost and storage estimates.