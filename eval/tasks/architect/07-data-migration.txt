Design a zero-downtime migration plan for adding a `tenant_id` column to all 50 tables in a PostgreSQL database serving live traffic at 2,000 writes/second. The migration must: add the column, backfill existing rows with the correct tenant_id, add NOT NULL constraint, and add indexes — all without downtime. Write your design to the output directory as data-migration.md. Evaluate: single deploy big-bang, expand-contract pattern, and dual-write with shadow reads. Include: rollback strategy, data validation, estimated migration duration, and risk assessment.