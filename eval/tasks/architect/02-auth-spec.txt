Write a technical specification for adding API key authentication to an existing public REST API. The API currently has no auth. Requirements: support multiple API keys per organization, rate limiting per key, key rotation without downtime, and an admin endpoint to manage keys. Write the spec to the output directory as auth-spec.md. Include: data model, endpoint designs, security considerations, migration plan from no-auth to auth, and trade-offs.