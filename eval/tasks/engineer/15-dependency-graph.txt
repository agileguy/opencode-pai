Create a TypeScript class `DependencyGraph<T>` with: `addNode(id: string, data: T)`, `addDependency(from: string, to: string)`, `getOrder(): string[]` (topological sort), `getDependencies(id: string): string[]`, and `hasCycle(): boolean`. Use TDD — write tests first covering: linear chain, diamond dependency, cycle detection, no dependencies returns any valid order, missing node throws, and complex graph with multiple valid orderings.