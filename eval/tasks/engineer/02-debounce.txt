Create a TypeScript function debounce(fn: Function, ms: number): Function that delays invoking fn until ms milliseconds after the last call. Use TDD — write the test file first (debounce.test.ts), then the implementation (debounce.ts).