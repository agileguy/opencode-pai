Create a TypeScript class `RateLimiter` implementing the token bucket algorithm. Constructor takes `{ maxTokens: number, refillRate: number, refillInterval: number }`. Methods: `tryConsume(tokens?: number): boolean`, `getTokens(): number`, `reset(): void`. Use TDD — write tests first covering: initial token count equals max, consume reduces tokens, consume fails when insufficient, tokens refill over time, cannot exceed max tokens, reset restores full tokens. Use timer mocks (vi.useFakeTimers or similar) for time-based tests.