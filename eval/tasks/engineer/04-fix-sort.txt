Fix the bugs in eval/fixtures/broken-sort.ts. There are two bugs: (1) it crashes when objects have missing keys, (2) it uses string comparison for numbers instead of numeric comparison. Write the fixed version as sortBy.ts and a regression test as sortBy.test.ts.