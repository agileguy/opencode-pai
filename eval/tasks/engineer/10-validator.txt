Create a TypeScript schema validator with a builder pattern: const schema = v.object({ name: v.string().minLength(1), age: v.number().min(0).max(150), email: v.string().matches(/^.+@.+\..+$/) }). The validate(data) method should return { valid: boolean, errors: string[] }. Use TDD — write tests first covering: valid object, missing fields, wrong types, constraint violations, nested validation errors, and empty object.