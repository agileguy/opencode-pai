Create a TypeScript function csvToJson(csv: string): object[] that parses a CSV string (with header row) into an array of objects. Handle quoted fields with commas inside them. Use TDD — write the test file first (csvToJson.test.ts), then the implementation (csvToJson.ts).