You have a monolithic TypeScript file at /workspace/eval/fixtures/monolith.ts that contains three classes (Logger, Cache, EventBus) in a single file. Decompose it into three separate module files (logger.ts, cache.ts, event-bus.ts) with proper imports between them, plus an index.ts that re-exports everything. Include tests that verify the refactored modules work identically. Delegate all implementation work to the appropriate PAI agent.